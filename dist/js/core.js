// declaring strict mode
"use strict";

// methods called in order of importance
// methods ordered by name
define([ 'Util' ], function( Util )
{
	var Core =
	{
		/**
		 * is the device retina?
		 * @type Boolean
		 */
		retinaDevice: false,

		init : function()
		{

		},
	};

	return Core;

});
